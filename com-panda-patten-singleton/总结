1、种单例写法的优、缺点。
 饿汉模式：
    优点： 线程安全，对用户使用较为方便。
    缺点： 无论是否使用都会优先加载出来，浪费内存
 懒汉模式：
    优点： 需要时才加载节约内存空间。
    缺点： 线程不安全
 注册式单例： 
    枚举： 推荐使用方式，线程安全，且无法被破坏。
    容器缓存： 
      优点：创建对象过多时使用。
      缺点：线程不安全。
2、思考破坏单例模式的方式有哪些？并且归纳总结。
     1.透过反射强行破坏 
     2.通过序列化写入磁盘，在反序列化读入内存，序列化破坏。
3、梳理内部类的执行逻辑。
      类的静态块----》 类的静态属性----》类内部类的属性----》类构造方法
